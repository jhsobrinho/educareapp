
import React from 'react';
import { Placeholder } from '../placeholders/SmartPEIPlaceholders';

export const StudentProfile: React.FC<any> = (props) => {
  return <Placeholder title="Student Profile" description="This component would display a student profile." {...props} />;
};

export default StudentProfile;

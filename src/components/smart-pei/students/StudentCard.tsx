
import React from 'react';
import { Placeholder } from '../placeholders/SmartPEIPlaceholders';

export const StudentCard: React.FC<any> = (props) => {
  return <Placeholder title="Student Card" description="This component would display a student card." {...props} />;
};

export default StudentCard;
